# barcode_scanner
Simple Streamlit barcode scanner app for inventory counting. Barcodes and SKUs
are stored as strings so leading zeros are preserved. The quantity column is
converted to a numeric type when loading previous counts to allow arithmetic
operations.
